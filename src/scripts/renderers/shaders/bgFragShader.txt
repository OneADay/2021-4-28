varying vec2 vUv;
varying vec3 vNormal;
uniform float delta;

void main() {
	float r = (1.0 - vUv.y) * 0.9;
	float g = (1.0 - vUv.y) * 0.7;
	float b = 0.5;
	float a = 1.0;
	
	r = clamp(r, 0.0, 0.9);
	g = clamp(g, 0.0, 0.5);
	gl_FragColor = vec4(r, g, b, a);
}